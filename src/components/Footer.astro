---
import GithubIcon   from "@assets/icons/github.svg";
import MailIcon     from "@assets/icons/mail.svg";
import LinkedinIcon from "@assets/icons/linkedin.svg";

interface Socials {
    name: string;
    href: string;
    logo: any;
}

const socials : [Socials] = [
    {name:"Github", href: "#", logo: GithubIcon },
    {name:"Mail", href: "#", logo: MailIcon },
    {name:"Linkedin", href: "#", logo: LinkedinIcon }
]

const year = new Date().getFullYear()
// <a href={s.href} class="items-center gap-2" key={s.name}>
// </a>
---
<footer class="flex flex-wrap justify-between items-center gap-4 py-4 px-10 max-w-screen bg-sky-100">
    <div class="flex justify-center items-center">
        © Yukna'vell {year}
    </div>

    <div class="flex justify-center items-center gap-4 flex-wrap">
        {socials.map(s => (
        <a href={s.href} class="flex items-center gap-2" key={s.name}>
            <s.logo title={s.name} class="w-6 h-6 flex-grow-0" /> 
        </a>
    ))}
    </div> 
</footer>
